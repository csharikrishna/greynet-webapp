# URL of the FastAPI inference backend (see ../backend). Leave unset to
# run the site in demo mode, where /lab returns clearly-labeled
# simulated results instead of real model output.
INFERENCE_API_URL=https://your-backend.example.com
